# 设计文档与实现计划只作本地工作文件，不进仓库
# docs/ 下只有 DESIGN.md 进仓库，其余（specs/、plans/）是本地的
/docs/*
!/docs/DESIGN.md
/.superpowers

# Python
__pycache__/
*.py[cod]
*.egg-info/

# 打包
build/
# dist/ 下的 build.py 与 touhou.spec 是要提交的源码，只忽略打包产物。
# 不要写 dist/*.spec —— 那条规则会连 touhou.spec 一起忽略掉，
# 导致 Plan D 里 git add dist/touhou.spec 静默失败（不报错，但文件没被跟踪）。
dist/*.exe

# 工具缓存
.mypy_cache/
.ruff_cache/
.pytest_cache/

# 虚拟环境
.venv/
venv/

# 杂项
*.log
